<header>
  <div class="header_outer_container">
    <div class="header_inner_container1">
      {% assign words = shop.name | split: ' ' %}
      <a href="{{ routes.root_url }}" class="header__title-link">
        <span>{{ words[0] }}</span>
        <span>{{ words[1] }}</span>
      </a>
    </div>

    <div class="header_inner_container2">
      <div class="header__menu-split">
        <div class="header__menu-empty"></div>
        <div class="header__menu-half">
          {% assign menu_links = section.settings.menu.links %}
          {% assign half = menu_links.size | divided_by: 2 %}
          {% for link in menu_links limit: half %}
            <a href="{{ link.url }}">{{ link.title }}</a>
          {% endfor %}
        </div>
        <div class="header__menu-half">
          {% assign menu_links = section.settings.menu.links %}
          {% assign half = menu_links.size | divided_by: 2 %}
          {% for link in menu_links offset: half %}
            <a href="{{ link.url }}">{{ link.title }}</a>
          {% endfor %}
        </div>
      </div>
    </div>

    <div class="header_inner_container3">
      {% if shop.customer_accounts_enabled %}
        <a href="{{ routes.account_url }}" class="header__icon-box">Account</a>
      {% endif %}
      <a href="{{ routes.cart_url }}" class="header__icon-box">
        Cart
        {% if cart.item_count > 0 %}
          <sup>{{ cart.item_count }}</sup>
        {% endif %}
      </a>
      <form action="{{ routes.search_url }}" method="get" class="header__icon-box header__search-box">
        <input type="search" name="q" placeholder="Search…" aria-label="Search" />
      </form>
    </div>
  </div>
</header>

{% stylesheet %}
  /* 1. Fixed header height */
  header {
    height: 7rem;
    min-width: 769px;
    display: flex;
    align-items: flex-start;
    margin: 2rem;
  }

  .header_outer_container {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: space-between;
    align-items: stretch;
  }

  /* 2. Make each inner container full height */
  .header_inner_container1,
  .header_inner_container2,
  .header_inner_container3 {
    height: 100%;
    display: flex;
  }

  .header_inner_container1 {
    flex: 0 0 40%;
  }

  .header_inner_container2 {
    flex: 0 0 30%;
  }

  .header_inner_container3 {
    flex: 0 0 30%;
    flex-direction: column;
    justify-content: space-between; /* first icon top, last icon bottom */
    align-items: flex-end;          /* align boxes to the right */
  }

  /* 3. Title: two spans hugging top & bottom, font auto‑scaling */
  .header__title-link {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    font-size: clamp(2.5rem, 5vw, 4rem);
    line-height: 1;
    color: var(--color-foreground);
    text-decoration: none;
    font-weight: bold;
  }
  .header__title-link span {
    display: block;
    width: 100%;
  }

  /* 4. Menu: split into two halves, links top & bottom */
  .header__menu-split {
    width: 100%;
    height: 100%;
    display: flex;
  }
  .header__menu-empty {
    flex: 1;
  }
  .header__menu-half {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: flex-start;
  }
  .header__menu-half a {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    line-height: 1;
    color: var(--color-foreground);
    text-decoration: none;
    padding: 0;
  }

  /* 5. Icon boxes: inline‑flex for content height, spaced by container */
  .header__icon-box {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1rem;
    border-radius: 0.25rem;
    text-decoration: none;
    font-weight: 600;
    border: 1px solid #000;
  }
  .header__icon-box sup {
    background: #fff;
    color: #000;
    border-radius: 50%;
    padding: 0 0.4em;
    font-size: 0.8em;
    margin-left: 0.5em;
  }

  /* 6. Search box styling */
  .header__search-box {
    padding: 0.5rem 1rem;
  }
  .header__search-box input[type="search"] {
    border: none;
    background: transparent;
    font-size: inherit;
    line-height: 1;
    width: 100%;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.header",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:labels.menu"
    }
  ]
}
{% endschema %}
