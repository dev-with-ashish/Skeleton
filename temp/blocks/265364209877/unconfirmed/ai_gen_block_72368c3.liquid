{% doc %}
  @prompt
    custom product section with its all variants option showing in each row updating product price with variant change, variant in its radio style
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-product-variants-{{ ai_gen_id }} {
    max-width: 100%;
    padding: {{ block.settings.section_padding }}px;
    background-color: {{ block.settings.background_color }};
    border-radius: {{ block.settings.border_radius }}px;
  }

  .ai-product-variants__container-{{ ai_gen_id }} {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .ai-product-variants__header-{{ ai_gen_id }} {
    text-align: center;
  }

  .ai-product-variants__title-{{ ai_gen_id }} {
    font-size: {{ block.settings.title_size }}px;
    color: {{ block.settings.text_color }};
    margin: 0 0 8px;
    font-weight: 600;
  }

  .ai-product-variants__description-{{ ai_gen_id }} {
    font-size: {{ block.settings.description_size }}px;
    color: {{ block.settings.text_color }};
    opacity: 0.8;
    margin: 0;
  }

  .ai-product-variants__product-info-{{ ai_gen_id }} {
    display: flex;
    gap: 24px;
    align-items: flex-start;
  }

  .ai-product-variants__image-{{ ai_gen_id }} {
    flex: 0 0 300px;
    width: 300px;
    height: 300px;
    border-radius: {{ block.settings.image_border_radius }}px;
    overflow: hidden;
  }

  .ai-product-variants__image-{{ ai_gen_id }} img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ai-product-variants__image-placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ai-product-variants__image-placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 200px;
    max-height: 200px;
  }

  .ai-product-variants__empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 14px;
    color: #666;
    text-align: center;
  }

  .ai-product-variants__details-{{ ai_gen_id }} {
    flex: 1;
    min-width: 0;
  }

  .ai-product-variants__product-title-{{ ai_gen_id }} {
    font-size: {{ block.settings.product_title_size }}px;
    color: {{ block.settings.text_color }};
    margin: 0 0 12px;
    font-weight: 600;
  }

  .ai-product-variants__price-{{ ai_gen_id }} {
    font-size: {{ block.settings.price_size }}px;
    color: {{ block.settings.price_color }};
    font-weight: 700;
    margin-bottom: 24px;
  }

  .ai-product-variants__variants-{{ ai_gen_id }} {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .ai-product-variants__variant-row-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 12px 16px;
    border: 2px solid {{ block.settings.variant_border_color }};
    border-radius: {{ block.settings.variant_border_radius }}px;
    cursor: pointer;
    transition: all 0.3s ease;
    background-color: {{ block.settings.variant_background_color }};
    position: relative;
  }

  .ai-product-variants__variant-row-{{ ai_gen_id }}:hover {
    border-color: {{ block.settings.variant_hover_border_color }};
    background-color: {{ block.settings.variant_hover_background_color }};
  }

  .ai-product-variants__variant-row-{{ ai_gen_id }}.selected {
    border-color: {{ block.settings.variant_selected_border_color }};
    background-color: {{ block.settings.variant_selected_background_color }};
  }

  .ai-product-variants__variant-radio-{{ ai_gen_id }} {
    width: {{ block.settings.radio_size }}px;
    height: {{ block.settings.radio_size }}px;
    margin: 0;
    cursor: pointer;
    accent-color: {{ block.settings.radio_selected_color }};
    flex-shrink: 0;
  }

  .ai-product-variants__variant-label-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    gap: 16px;
    cursor: pointer;
    width: 100%;
  }

  .ai-product-variants__variant-info-{{ ai_gen_id }} {
    flex: 1;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .ai-product-variants__variant-details-{{ ai_gen_id }} {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .ai-product-variants__variant-title-{{ ai_gen_id }} {
    font-size: {{ block.settings.variant_title_size }}px;
    color: {{ block.settings.text_color }};
    font-weight: 500;
    margin: 0;
  }

  .ai-product-variants__variant-availability-{{ ai_gen_id }} {
    font-size: 12px;
    padding: 4px 8px;
    border-radius: 12px;
    font-weight: 500;
    display: inline-block;
  }

  .ai-product-variants__variant-availability-{{ ai_gen_id }}.in-stock {
    background-color: #e8f5e8;
    color: #2d5a2d;
  }

  .ai-product-variants__variant-availability-{{ ai_gen_id }}.out-of-stock {
    background-color: #ffeaea;
    color: #d32f2f;
  }

  .ai-product-variants__variant-price-{{ ai_gen_id }} {
    font-size: {{ block.settings.variant_price_size }}px;
    color: {{ block.settings.price_color }};
    font-weight: 600;
  }

  .ai-product-variants__add-to-cart-{{ ai_gen_id }} {
    margin-top: 24px;
  }

  .ai-product-variants__add-button-{{ ai_gen_id }} {
    width: 100%;
    padding: 16px 24px;
    background-color: {{ block.settings.button_color }};
    color: {{ block.settings.button_text_color }};
    border: none;
    border-radius: {{ block.settings.button_border_radius }}px;
    font-size: {{ block.settings.button_text_size }}px;
    font-weight: 600;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .ai-product-variants__add-button-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.button_hover_color }};
  }

  .ai-product-variants__add-button-{{ ai_gen_id }}:disabled {
    background-color: #cccccc;
    cursor: not-allowed;
  }

  .ai-product-variants__empty-product-{{ ai_gen_id }} {
    text-align: center;
    padding: 40px 20px;
    color: #666;
    font-style: italic;
  }

  .ai-product-variants__variant-row-{{ ai_gen_id }}.disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  .ai-product-variants__variant-row-{{ ai_gen_id }}.disabled .ai-product-variants__variant-radio-{{ ai_gen_id }} {
    cursor: not-allowed;
  }

  .ai-product-variants__variant-row-{{ ai_gen_id }}.disabled .ai-product-variants__variant-label-{{ ai_gen_id }} {
    cursor: not-allowed;
  }

  @media screen and (max-width: 768px) {
    .ai-product-variants__product-info-{{ ai_gen_id }} {
      flex-direction: column;
      gap: 16px;
    }

    .ai-product-variants__image-{{ ai_gen_id }} {
      flex: none;
      width: 100%;
      max-width: 300px;
      margin: 0 auto;
    }

    .ai-product-variants__variant-info-{{ ai_gen_id }} {
      flex-direction: column;
      align-items: flex-start;
      gap: 8px;
    }

    .ai-product-variants__variant-details-{{ ai_gen_id }} {
      width: 100%;
    }
  }
{% endstyle %}

<product-variants-{{ ai_gen_id }}
  class="ai-product-variants-{{ ai_gen_id }}"
  {{ block.shopify_attributes }}
>
  <div class="ai-product-variants__container-{{ ai_gen_id }}">
    {% if block.settings.title != blank or block.settings.description != blank %}
      <div class="ai-product-variants__header-{{ ai_gen_id }}">
        {% if block.settings.title != blank %}
          <h2 class="ai-product-variants__title-{{ ai_gen_id }}">{{ block.settings.title }}</h2>
        {% endif %}
        {% if block.settings.description != blank %}
          <p class="ai-product-variants__description-{{ ai_gen_id }}">{{ block.settings.description }}</p>
        {% endif %}
      </div>
    {% endif %}

    {% if block.settings.product != blank %}
      {% assign product = block.settings.product %}
      
      <div class="ai-product-variants__product-info-{{ ai_gen_id }}">
        <div class="ai-product-variants__image-{{ ai_gen_id }}">
          {% if product.featured_image %}
            <img
              src="{{ product.featured_image | image_url: width: 600 }}"
              alt="{{ product.featured_image.alt | escape }}"
              loading="lazy"
              width="600"
              height="600"
              id="ai-product-image-{{ ai_gen_id }}"
            >
          {% else %}
            <div class="ai-product-variants__image-placeholder-{{ ai_gen_id }}">
              {{ 'product-1' | placeholder_svg_tag }}
            </div>
          {% endif %}
        </div>

        <div class="ai-product-variants__details-{{ ai_gen_id }}">
          <h3 class="ai-product-variants__product-title-{{ ai_gen_id }}">{{ product.title }}</h3>
          
          <div class="ai-product-variants__price-{{ ai_gen_id }}" id="ai-current-price-{{ ai_gen_id }}">
            {{ product.price | money }}
          </div>

          {% if product.variants.size > 1 %}
            <form action="/cart/add" method="post" enctype="multipart/form-data" id="ai-variant-form-{{ ai_gen_id }}">
              <div class="ai-product-variants__variants-{{ ai_gen_id }}">
                {% for variant in product.variants %}
                  <div
                    class="ai-product-variants__variant-row-{{ ai_gen_id }} {% unless variant.available %}disabled{% endunless %}"
                    data-variant-id="{{ variant.id }}"
                    data-variant-price="{{ variant.price }}"
                    data-variant-available="{{ variant.available }}"
                    {% if variant.featured_image %}
                      data-variant-image="{{ variant.featured_image | image_url: width: 600 }}"
                    {% endif %}
                  >
                    <label class="ai-product-variants__variant-label-{{ ai_gen_id }}" for="variant-{{ variant.id }}-{{ ai_gen_id }}">
                      <input
                        type="radio"
                        id="variant-{{ variant.id }}-{{ ai_gen_id }}"
                        name="id"
                        value="{{ variant.id }}"
                        class="ai-product-variants__variant-radio-{{ ai_gen_id }}"
                        {% if forloop.first %}checked{% endif %}
                        {% unless variant.available %}disabled{% endunless %}
                      >
                      
                      <div class="ai-product-variants__variant-info-{{ ai_gen_id }}">
                        <div class="ai-product-variants__variant-details-{{ ai_gen_id }}">
                          <div class="ai-product-variants__variant-title-{{ ai_gen_id }}">
                            {{ variant.title }}
                          </div>
                          <div class="ai-product-variants__variant-availability-{{ ai_gen_id }} {% if variant.available %}in-stock{% else %}out-of-stock{% endif %}">
                            {% if variant.available %}In Stock{% else %}Out of Stock{% endif %}
                          </div>
                        </div>
                        <div class="ai-product-variants__variant-price-{{ ai_gen_id }}">
                          {{ variant.price | money }}
                        </div>
                      </div>
                    </label>
                  </div>
                {% endfor %}
              </div>

              <div class="ai-product-variants__add-to-cart-{{ ai_gen_id }}">
                <button
                  type="submit"
                  class="ai-product-variants__add-button-{{ ai_gen_id }}"
                  id="ai-add-to-cart-{{ ai_gen_id }}"
                  {% unless product.variants.first.available %}disabled{% endunless %}
                >
                  {% if product.variants.first.available %}
                    {{ block.settings.add_to_cart_text }}
                  {% else %}
                    Sold Out
                  {% endif %}
                </button>
              </div>
            </form>
          {% else %}
            <div class="ai-product-variants__add-to-cart-{{ ai_gen_id }}">
              <form action="/cart/add" method="post" enctype="multipart/form-data">
                <input type="hidden" name="id" value="{{ product.variants.first.id }}">
                <button
                  type="submit"
                  class="ai-product-variants__add-button-{{ ai_gen_id }}"
                  {% unless product.available %}disabled{% endunless %}
                >
                  {% if product.available %}
                    {{ block.settings.add_to_cart_text }}
                  {% else %}
                    Sold Out
                  {% endif %}
                </button>
              </form>
            </div>
          {% endif %}
        </div>
      </div>
    {% else %}
      <div class="ai-product-variants__empty-product-{{ ai_gen_id }}">
        Next, select a product to display its variants
      </div>
    {% endif %}
  </div>
</product-variants-{{ ai_gen_id }}>

<script>
  (function() {
    class ProductVariants{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
      }

      connectedCallback() {
        this.radioButtons = this.querySelectorAll('.ai-product-variants__variant-radio-{{ ai_gen_id }}');
        this.variantRows = this.querySelectorAll('.ai-product-variants__variant-row-{{ ai_gen_id }}');
        this.priceElement = this.querySelector('#ai-current-price-{{ ai_gen_id }}');
        this.addToCartButton = this.querySelector('#ai-add-to-cart-{{ ai_gen_id }}');
        this.productImage = this.querySelector('#ai-product-image-{{ ai_gen_id }}');

        this.setupEventListeners();
        this.updateSelectedRow();
      }

      setupEventListeners() {
        this.radioButtons.forEach((radio) => {
          radio.addEventListener('change', () => {
            if (radio.checked) {
              this.updateVariantSelection(radio);
            }
          });
        });

        this.variantRows.forEach((row) => {
          row.addEventListener('click', (e) => {
            if (e.target.type !== 'radio') {
              const radio = row.querySelector('.ai-product-variants__variant-radio-{{ ai_gen_id }}');
              if (radio && !radio.disabled) {
                radio.checked = true;
                this.updateVariantSelection(radio);
              }
            }
          });
        });
      }

      updateVariantSelection(selectedRadio) {
        const selectedRow = selectedRadio.closest('.ai-product-variants__variant-row-{{ ai_gen_id }}');
        
        this.variantRows.forEach((row) => {
          row.classList.remove('selected');
        });

        selectedRow.classList.add('selected');

        const variantPrice = selectedRow.dataset.variantPrice;
        const variantAvailable = selectedRow.dataset.variantAvailable === 'true';
        const variantImage = selectedRow.dataset.variantImage;

        if (this.priceElement && variantPrice) {
          const formattedPrice = new Intl.NumberFormat('en-IN', {
            style: 'currency',
            currency: 'INR'
          }).format(variantPrice / 100);
          this.priceElement.textContent = formattedPrice;
        }

        if (this.addToCartButton) {
          if (variantAvailable) {
            this.addToCartButton.disabled = false;
            this.addToCartButton.textContent = '{{ block.settings.add_to_cart_text }}';
          } else {
            this.addToCartButton.disabled = true;
            this.addToCartButton.textContent = 'Sold Out';
          }
        }

        if (this.productImage && variantImage) {
          this.productImage.src = variantImage;
        }
      }

      updateSelectedRow() {
        const checkedRadio = this.querySelector('.ai-product-variants__variant-radio-{{ ai_gen_id }}:checked');
        if (checkedRadio) {
          this.updateVariantSelection(checkedRadio);
        }
      }
    }

    customElements.define('product-variants-{{ ai_gen_id }}', ProductVariants{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Product variants display",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Product"
    },
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section title",
      "default": "Choose Your Variant"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Select from available product variants below"
    },
    {
      "type": "text",
      "id": "add_to_cart_text",
      "label": "Add to cart button text",
      "default": "Add to Cart"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "price_color",
      "label": "Price color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "variant_background_color",
      "label": "Variant background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "variant_hover_background_color",
      "label": "Variant hover background color",
      "default": "#f8f8f8"
    },
    {
      "type": "color",
      "id": "variant_selected_background_color",
      "label": "Selected variant background color",
      "default": "#f0f8ff"
    },
    {
      "type": "color",
      "id": "variant_border_color",
      "label": "Variant border color",
      "default": "#e0e0e0"
    },
    {
      "type": "color",
      "id": "variant_hover_border_color",
      "label": "Variant hover border color",
      "default": "#cccccc"
    },
    {
      "type": "color",
      "id": "variant_selected_border_color",
      "label": "Selected variant border color",
      "default": "#007acc"
    },
    {
      "type": "color",
      "id": "radio_selected_color",
      "label": "Radio button selected color",
      "default": "#007acc"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button color",
      "default": "#007acc"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_hover_color",
      "label": "Button hover color",
      "default": "#005a99"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 16,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "Section title size",
      "default": 32
    },
    {
      "type": "range",
      "id": "description_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Description size",
      "default": 16
    },
    {
      "type": "range",
      "id": "product_title_size",
      "min": 18,
      "max": 36,
      "step": 2,
      "unit": "px",
      "label": "Product title size",
      "default": 24
    },
    {
      "type": "range",
      "id": "price_size",
      "min": 16,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Price size",
      "default": 20
    },
    {
      "type": "range",
      "id": "variant_title_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Variant title size",
      "default": 16
    },
    {
      "type": "range",
      "id": "variant_price_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Variant price size",
      "default": 14
    },
    {
      "type": "range",
      "id": "button_text_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Button text size",
      "default": 16
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "section_padding",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "label": "Section padding",
      "default": 32
    },
    {
      "type": "range",
      "id": "border_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Section border radius",
      "default": 8
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Image border radius",
      "default": 8
    },
    {
      "type": "range",
      "id": "variant_border_radius",
      "min": 0,
      "max": 16,
      "step": 2,
      "unit": "px",
      "label": "Variant row border radius",
      "default": 6
    },
    {
      "type": "range",
      "id": "button_border_radius",
      "min": 0,
      "max": 16,
      "step": 2,
      "unit": "px",
      "label": "Button border radius",
      "default": 6
    },
    {
      "type": "range",
      "id": "radio_size",
      "min": 16,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Radio button size",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Product variants display"
    }
  ]
}
{% endschema %}